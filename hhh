<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>To-Do List</title>
  <style>
    :root{
      --blue:#1e73ff;
      --blue2:#2f86ff;
      --text:#111;
      --muted:#666;
      --border:#d8d8d8;
      --bg:#f3f4f6;
      --shadow: 0 8px 24px rgba(0,0,0,.12);
      --radius:18px;
    }

    *{ box-sizing:border-box; }
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, "Noto Sans KR", sans-serif;
      background: var(--bg);
      color:var(--text);
    }

    .wrap{
      max-width: 860px;
      margin: 40px auto;
      padding: 0 18px;
    }

    .card{
      background:#fff;
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 34px 26px 26px;
    }

    h1{
      margin: 0 0 22px;
      text-align:center;
      font-size: 44px;
      letter-spacing: .5px;
      font-weight: 800;
    }

    .form{
      width: min(520px, 100%);
      margin: 0 auto 18px;
      display:flex;
      flex-direction:column;
      gap:14px;
      align-items:center;
    }

    .row{
      width:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      gap:10px;
    }

    .label{
      width: 110px;
      text-align:right;
      font-size: 18px;
      color:#111;
    }

    input[type="text"]{
      width: 320px;
      height: 34px;
      padding: 6px 10px;
      border: 2px solid #bcbcbc;
      border-radius: 3px;
      outline:none;
      font-size: 16px;
    }
    input[type="text"]:focus{
      border-color:#8bb4ff;
    }

    select{
      width: 140px;
      height: 34px;
      border: 2px solid #bcbcbc;
      border-radius: 4px;
      padding: 2px 8px;
      font-size: 16px;
      background:#fff;
      outline:none;
    }

    .btn{
      border:0;
      cursor:pointer;
      user-select:none;
      transition: transform .05s ease, filter .15s ease;
    }
    .btn:active{ transform: translateY(1px); }

    .btn-primary{
      margin-top: 2px;
      width: 150px;
      height: 46px;
      background: var(--blue);
      color:#fff;
      border-radius: 999px;
      font-size: 18px;
      font-weight: 700;
      box-shadow: 0 10px 20px rgba(30,115,255,.25);
    }
    .btn-primary:hover{ filter: brightness(1.03); }

    .filters{
      display:flex;
      justify-content:center;
      gap:16px;
      margin: 18px 0 22px;
    }

    .filter{
      width: 104px;
      height: 46px;
      border-radius: 999px;
      background: var(--blue);
      color:#fff;
      font-size: 18px;
      font-weight: 800;
      display:flex;
      align-items:center;
      justify-content:center;
      gap:8px;
      box-shadow: 0 10px 20px rgba(30,115,255,.20);
    }
    .filter.active{
      background: var(--blue2);
      outline: 3px solid rgba(30,115,255,.18);
    }

    .list{
      margin-top: 6px;
      border-top: 1px solid #eee;
      padding-top: 18px;
    }

    .item{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 14px;
      padding: 10px 6px;
    }

    .left{
      display:flex;
      align-items:center;
      gap:10px;
      min-width: 260px;
    }

    .task-text{
      font-size: 22px;
    }

    .task-text.done{
      color: #888;
      text-decoration: line-through;
    }

    .right{
      display:flex;
      align-items:center;
      gap: 14px;
    }

    .btn-edit{
      width: 140px;
      height: 44px;
      background: var(--blue);
      color:#fff;
      border-radius: 999px;
      font-size: 18px;
      font-weight: 800;
      box-shadow: 0 10px 20px rgba(30,115,255,.18);
    }

    .btn-del{
      width: 72px;
      height: 44px;
      background:#f3f3f3;
      color:#111;
      border: 2px solid #bcbcbc;
      border-radius: 4px;
      font-size: 16px;
      font-weight: 700;
    }

    .empty{
      padding: 18px 6px;
      color: var(--muted);
      font-size: 16px;
      text-align:center;
    }

    @media (max-width: 520px){
      h1{ font-size: 34px; }
      .label{ width: 92px; font-size: 16px; }
      input[type="text"]{ width: 100%; }
      .row{ justify-content:space-between; }
      .left{ min-width: 0; }
      .task-text{ font-size: 18px; }
      .btn-edit{ width: 110px; }
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>To-Do List</h1>

      <div class="form">
        <div class="row">
          <div class="label">할 일 작성:</div>
          <input id="taskInput" type="text" placeholder="" />
        </div>

        <div class="row">
          <div class="label">우선순위:</div>
          <select id="prioritySelect">
            <option value="낮음" selected>낮음</option>
            <option value="보통">보통</option>
            <option value="높음">높음</option>
          </select>
        </div>

        <button id="addBtn" class="btn btn-primary">할 일 추가</button>
      </div>

      <div class="filters">
        <button class="btn filter active" data-filter="all">
          <span>✓</span><span>전체</span>
        </button>
        <button class="btn filter" data-filter="done">해결</button>
        <button class="btn filter" data-filter="todo">미해결</button>
      </div>

      <div id="list" class="list"></div>
    </div>
  </div>

  <script>
    // ====== Data ======
    const STORAGE_KEY = "todo_like_ui_v1";
    let todos = load();
    let filter = "all";

    // ====== Elements ======
    const $input = document.getElementById("taskInput");
    const $priority = document.getElementById("prioritySelect");
    const $add = document.getElementById("addBtn");
    const $list = document.getElementById("list");
    const $filters = Array.from(document.querySelectorAll(".filter"));

    // ====== Init ======
    render();

    // Enter로 추가
    $input.addEventListener("keydown", (e) => {
      if (e.key === "Enter") addTodo();
    });
    $add.addEventListener("click", addTodo);

    $filters.forEach(btn => {
      btn.addEventListener("click", () => {
        filter = btn.dataset.filter;
        $filters.forEach(b => b.classList.toggle("active", b === btn));
        render();
      });
    });

    // ====== Functions ======
    function addTodo(){
      const text = $input.value.trim();
      if(!text) return;

      todos.unshift({
        id: crypto.randomUUID(),
        text,
        priority: $priority.value,
        done: false,
        createdAt: Date.now()
      });
      save();
      $input.value = "";
      render();
      $input.focus();
    }

    function toggleDone(id){
      todos = todos.map(t => t.id === id ? ({...t, done: !t.done}) : t);
      save();
      render();
    }

    function deleteTodo(id){
      todos = todos.filter(t => t.id !== id);
      save();
      render();
    }

    function editTodo(id){
      const t = todos.find(x => x.id === id);
      if(!t) return;

      // 간단하게 prompt로 수정 (UI 똑같이 유지하려고)
      const next = prompt("할 일을 수정하세요:", t.text);
      if(next === null) return; // 취소
      const trimmed = next.trim();
      if(!trimmed) return;

      todos = todos.map(x => x.id === id ? ({...x, text: trimmed}) : x);
      save();
      render();
    }

    function getFiltered(){
      if(filter === "done") return todos.filter(t => t.done);
      if(filter === "todo") return todos.filter(t => !t.done);
      return todos;
    }

    function render(){
      const data = getFiltered();

      if(data.length === 0){
        $list.innerHTML = `<div class="empty">표시할 항목이 없습니다.</div>`;
        return;
      }

      $list.innerHTML = data.map(t => `
        <div class="item">
          <div class="left">
            <input type="checkbox" ${t.done ? "checked" : ""} data-action="toggle" data-id="${t.id}"/>
            <div class="task-text ${t.done ? "done" : ""}">
              ${escapeHtml(t.text)} (${escapeHtml(t.priority)})
            </div>
          </div>

          <div class="right">
            <button class="btn btn-edit" data-action="edit" data-id="${t.id}">수정</button>
            <button class="btn btn-del" data-action="del" data-id="${t.id}">삭제</button>
          </div>
        </div>
      `).join("");

      // 이벤트 위임
      $list.querySelectorAll("[data-action]").forEach(el => {
        const id = el.dataset.id;
        const action = el.dataset.action;

        if(action === "toggle"){
          el.addEventListener("change", () => toggleDone(id));
        }else if(action === "edit"){
          el.addEventListener("click", () => editTodo(id));
        }else if(action === "del"){
          el.addEventListener("click", () => deleteTodo(id));
        }
      });
    }

    function save(){
      localStorage.setItem(STORAGE_KEY, JSON.stringify(todos));
    }

    function load(){
      try{
        return JSON.parse(localStorage.getItem(STORAGE_KEY)) ?? [];
      }catch{
        return [];
      }
    }

    function escapeHtml(str){
      return str
        .replaceAll("&", "&amp;")
        .replaceAll("<", "&lt;")
        .replaceAll(">", "&gt;")
        .replaceAll('"', "&quot;")
        .replaceAll("'", "&#039;");
    }
  </script>
</body>
</html>
